# =============================================================================
# DriveVectorAI Environment Configuration
# =============================================================================
# For Coolify deployment, set these as environment variables in the Coolify UI
# For local development, copy this file to .env and fill in the values
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL database settings
DB_NAME=drivevectorai
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=db
DB_PORT=5432

# -----------------------------------------------------------------------------
# GOOGLE CLOUD CONFIGURATION
# -----------------------------------------------------------------------------
# Your Google Cloud Project ID (REQUIRED)
GOOGLE_PROJECT_ID=your-gcp-project-id

# Google Cloud Secret Manager (OPTIONAL - only if using Secret Manager for DB credentials)
# If set, this will override the direct DB_* variables above
SECRET_MANAGER_DB_SECRET_ID=

# Google Drive folder ID (optional - can be set per ingestion job)
DRIVE_FOLDER_ID=

# Google Cloud Storage bucket (optional)
GCS_BUCKET_NAME=

# Google Application Credentials path (for local development only)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# -----------------------------------------------------------------------------
# REDIS CONFIGURATION
# -----------------------------------------------------------------------------
REDIS_BROKER_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# CELERY CONFIGURATION
# -----------------------------------------------------------------------------
# Number of concurrent Celery workers
CELERY_CONCURRENCY=4

# -----------------------------------------------------------------------------
# SERVICE PORTS (for local development)
# -----------------------------------------------------------------------------
FRONTEND_PORT=3000
BACKEND_PORT=8000

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
REACT_APP_API_URL=/api

# =============================================================================
# COOLIFY DEPLOYMENT NOTES
# =============================================================================
# 1. Set all required environment variables in Coolify's environment settings
# 2. Ensure GOOGLE_PROJECT_ID is set
# 3. Either set DB_* variables OR SECRET_MANAGER_DB_SECRET_ID
# 4. For Google Cloud authentication, you may need to:
#    - Use Workload Identity (recommended for production)
#    - Or mount service account credentials as a secret
# 5. Coolify will automatically use these variables, no .env file needed
# =============================================================================
